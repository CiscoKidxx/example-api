{
  "name": "sample_api",
  "version": "1.0.0",
  "description": "Chris' sample sls / lambda / dynamodb project",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Newell",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.248.1",
    "bluebird": "^3.5.1"
  }
}
